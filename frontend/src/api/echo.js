/**
 * DEPRECATED: Use useEcho() hook from @/hooks/useEcho instead
 * 
 * This file is kept for backwards compatibility but should not be used
 * in new code. The hook-based approach properly handles:
 * - Server-side rendering (SSR)
 * - Hydration timing
 * - Stale closure issues
 * - Echo instance lifecycle
 * 
 * Import from hooks instead:
 * import { useEcho, useEchoListener } from '@/hooks/useEcho';
 */

export default null;